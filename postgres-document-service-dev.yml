server:
  port: 9020

# Mongo Configuration
spring:
  data:
    mongodb:
      uri: mongodb:/agutierrezl:agutierrezl@localhost:27017/document-service-db?authSource=admin
# Mongo Configuration
  jpa:
    databasePlatform:
      hibernate:
        ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          format_sql: true
          dialect: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:postgresql://localhost:5432/agutierrezl
    username: agutierrezl
    password: agutierrezl
